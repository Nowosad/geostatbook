language: r
sudo: required
warnings_are_errors: true
env:
  global:
    - NOT_CRAN=true
before_install: 
  - echo "options(repos = c(CRAN='http://cran.rstudio.com'))" > ~/.Rprofile
apt_packages:
 - r-cran-rgdal
 - r-cran-ncdf4